import { SDSConnection } from "../sds/SDSConnection";
import { JANUSClass } from "./JANUSClass";
/**
 * Array of name - value pairs which describe the parameters.
 * The name and the values must be described as strings in the following order:
 * the first element is the name of the parameter, the second element is the value.
 */
declare type ScriptParameter = string[];
export declare class CustomOperations extends JANUSClass {
    constructor(sdsConnection: SDSConnection);
    /**
     * Executes the passed script on the server
     * @param sourceCode The complete script that should be executed on the server.
     * @param scriptParam Array of name - value pairs which describe the parameters as strings.
     */
    runScriptOnServer(sourceCode: string, scriptParam?: ScriptParameter): Promise<string>;
}
export {};
