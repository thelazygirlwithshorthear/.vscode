import { SDSConnection } from "../sds/SDSConnection";
import { JANUSClass } from "./JANUSClass";
export declare class PDTools extends JANUSClass {
    static NEW_VERSION: string;
    static UTF8: boolean;
    constructor(sdsConnection: SDSConnection);
    receiveFile(remoteFileName: string, localFileName: string): Promise<void>;
    /**
     * @param remoteFileName Path where the file should be stored on the server
     * @param localFileName Local file path to the file to transfer
     * @param newName true indicates that a new filename is generated if remoteFileName already exists
     */
    sendFile(remoteFileName: string, localFileName: string, newName?: boolean): Promise<string>;
}
