export declare enum LogLevel {
    Debug = 0,
    Info = 1,
    Warn = 2,
    Error = 3,
    None = 4
}
export declare function LogLevelFromString(logLevelString: string): LogLevel;
export interface LogConfiguration {
    /** The name of the logfile. */
    fileName?: string;
    /** The minimum loglevel(s) for messages written to the logfile. */
    logLevel?: {
        [logName: string]: LogLevel;
    };
}
export declare class Logger {
    private name;
    static fd: number | undefined;
    static create(name: string): Logger;
    private static loggers;
    private static _config;
    private static startTime;
    private logLevel;
    constructor(name: string);
    debug(msg: string): void;
    info(msg: string): void;
    warn(msg: string): void;
    error(msg: string): void;
    static set config(newConfig: LogConfiguration);
    private log;
    private configure;
}
