"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("../utils");
const comments_1 = require("../utils/comments");
const parserFactory = ({ customTags }) => {
    const regex = (0, utils_1.getRegex)(customTags);
    const commentsRegex = new RegExp(`^\\s*\\/\\/-?${regex}$`, 'mig');
    return (contents, file) => {
        return (0, comments_1.extractSingleLineComments)(contents, file, commentsRegex);
    };
};
exports.default = parserFactory;
